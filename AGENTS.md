# AGENTS.md

## 同步上游代码规则

- 始终从 `upstream` 拉取上游代码，只将本地修改推送到自己的 `origin`，绝不将任何修改推送到 `upstream`。
- 合并或变基上游代码时，必须保留本仓库的二开功能和本地定制逻辑。
- 如果发生冲突，不能机械选择某一方；必须逐处核对冲突原因、上下文行为和二开改动意图后再合并。
- 解决冲突后，需要检查相关功能路径，确认上游更新没有覆盖或破坏二开功能。
- 提交前先查看 `git diff` 和 `git status`，确认只包含预期变更。
